# Changelog

本项目的所有重要变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

## [0.1.0] - 2026-08-12

首个公开发布版本。

### 新增

- **每日任务**：独立运行页执行 `maa run daily -v`，进度与日志分离展示（如 `当前进度：[1/7] 启动游戏`）；运行中显示明确的停止操作；`SIGTERM` 后超时使用 `SIGKILL` 清理整个进程组
- **daily 配置管理**：自动定位 `~/.config/maa/tasks/` 下唯一的 TOML / YAML / JSON 配置文件；支持任务启停、上移下移、新增删除、编辑名称与基础/高级参数；客户端、连战次数、基建模式、无人机用途、临期理智药等使用带说明的选项
- **多选与条件**：招募星级、设施顺序支持多选；关卡字段按 `v` 可创建 `OnSideStory` 活动变体；编辑、移动和删除条件变体（基础条件可编辑，`And`/`Or`/`Not` 组合原样保留）
- **自动战斗**：单作业与作业集（批量）分开保存；作业集支持独立启停、排序、批量运行、二次确认清空与删除、切换运行页签；`prts://` 与 `maa://` 协议解析
- **运行设置持久化**：共享自动编队、助战、理智药等运行参数，修改后立即原子保存，重启后继续复用
- **运行进度**：单作业、单独运行的批量条目和完整作业集均显示实时进度；三星结算后自动停用并保存；失败时 MaaCore 立即中止且不影响后续条目
- **关卡选择器**：常驻关卡、活动热更新目录、按接口时区计算的开放状态和掉落说明，也可输入自定义关卡；`当前/上次作战` 保存空 `stage`
- **关卡码别名**：启动/热更新成功后，按 `overview.json` 为缺失前缀的 code 自动生成符号链接（如 `TO-1.json` → `act53side_01-....json`）
- **更新管理**：手动二次确认的「仅更新活动与导航资源」与「更新 MaaCore 与基础资源」入口，复用任务日志与停止控制，不阻塞 TUI 事件循环
- **分级日志**：强制 `MAA_LOG_PREFIX=Always` 解析日志等级，按 Info / Success / Warn / Error / Debug 分级着色，颜色使用固定 RGB 避免终端主题映射
- **快捷键帮助**：底部状态栏按页面显示高优先级操作，`?` 打开当前页面完整帮助；弹窗打开时切换为弹窗自身的操作提示
- **安全写入**：配置先写同目录临时文件再原子替换，保留 TOML 注释、布局与未知字段

### 变更

- 主菜单与配置编辑页不再挤占日志区域，日志展示与任务控制相互独立

### 修复

- maa-cli 写入 `stderr` 的普通日志不再一律显示成黄色 `!`
- 升级旧版缓存时清空旧单作业列表并提示重新搜索；已有作业集条目的顺序、难度和启用状态保持不变
- 批量进度按当前 maa 子进程 PID 过滤日志，监视失效时停止批次，避免错误停用项目